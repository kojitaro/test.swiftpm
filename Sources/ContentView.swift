import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Button("OK") {
                // ボタンタップ時の処理
            }
            .buttonStyle(.borderedProminent)
        }
    }
}

#Preview {
    ContentView()
}
